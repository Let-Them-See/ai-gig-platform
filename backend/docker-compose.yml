version: '3.8'
services:
  mongo:
    image: mongo:6.0
    container_name: ai_gig_mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    environment:
      MONGO_INITDB_DATABASE: ai_gig_platform
volumes:
  mongo-data:
